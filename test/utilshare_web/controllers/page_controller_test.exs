defmodule UtilshareWeb.PageControllerTest do
  use UtilshareWeb.ConnCase

  #Vue Renders our Pages
  # test "GET /", %{conn: conn} do
  #   conn = get conn, "/"
  #   assert html_response(conn, 200) =~ "Welcome to Phoenix!"
  # end
end
